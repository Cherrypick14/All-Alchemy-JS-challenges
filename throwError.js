// Throwing an error means we stop runnig a code art that point
// We create an object instance using the new keyword

function throwError() {
    throw new Error("I got you Error")
}

module.exports = throwError;
